{
  "routes": [
    {
      "method": "GET",
      "path": "/front-page",
      "handler": "front-page.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/front-page",
      "handler": "front-page.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/front-page",
      "handler": "front-page.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
